# Production Environment Configuration for Frontend
# Copy this file to .env.local and update all values before deployment

# NextAuth Configuration
# Replace with your actual server URL
NEXTAUTH_URL=http://192.168.x.x
# or for HTTPS (recommended):
# NEXTAUTH_URL=https://yourdomain.com

# Generate new secret: openssl rand -base64 32
NEXTAUTH_SECRET='CHANGE_THIS_TO_NEW_RANDOM_SECRET'

# Django Backend API URL
# For production on Dell R340, this should be localhost since both run on same server
NEXT_PUBLIC_Django_API_URL=http://localhost:8000
# Or if accessing through nginx reverse proxy:
# NEXT_PUBLIC_Django_API_URL=http://192.168.x.x/api
